# 实施规格：AI 用户画像（对话沉淀 → 管理端板块）

> 本文档是"操作说明 / 实施提示词"，描述要把这套功能做出来需要执行的步骤、关键决策与验收标准。
> 不绑定具体语言框架：下文按「数据层 / 领域层 / 服务层 / 接口层 / AI 层 / 前端 / 管理端」分层描述，
> 对应到你的技术栈即可。

---

## 〇、一句话需求

用户在 AI 对话页完成行程规划后，系统在**不打扰用户**的前提下从对话中沉淀出这个人的偏好（结构化标签+权重、固定字段、约束、一段 AI 文字画像），保存到库；管理端新增一个板块可以查看、筛选、导出、人工修正这些画像；下一次对话/规划时把画像回灌进 prompt，让 AI 不用重复追问已知偏好。用户端只暴露一个开关。

---

## 一、需求决策清单（**动手前必须先和需求方敲定，逐条确认，不要自己猜**）

这套功能有 12 个决策点会直接改变表结构和代码结构，任何一个猜错都要返工。逐条问：

| # | 决策点 | 备选项 | 我们的选择 |
|---|---|---|---|
| 1 | 画像挂在谁身上 | 只做登录用户 / 匿名设备ID / 双层+合并 | 必须登录才能对话；不做匿名，直接加 user_id |
| 2 | "对话完成"的触发时机 | 每轮回复后 / 前端上报 / 空闲超时 / 业务成功时 | **生成行程成功时** |
| 3 | 画像更新策略 | 每次整份重算 / 增量累积 / 只生成一份 | **增量累积标签权重 + 定期重写摘要** |
| 4 | 是否闭环回灌 prompt | 只沉淀不回灌 / 回灌 / 回灌+AI 显式确认 / 只回灌表单 | **回灌到对话与规划的 prompt** |
| 5 | 匿名与登录边界 | 游客可聊生成时登录 / 必须登录 / 不强制 | **必须登录（前后端都拦）** |
| 6 | 标签增量在哪个时点写 | 每轮规则累加 / 每次调模型抽取 / 只业务成功时算 | **每轮对话用规则累加（不调模型）** |
| 7 | 摘要重写周期 | 每次业务成功 / 满 N 轮或 M 天 / 变化超阈值 / 手动 | **每次生成行程成功就重写** |
| 8 | 摘要模型通道 | 共用现有模型 / 同服务新端点隔离模型 / 直连新 provider | **后端直连新 provider（独立模型）** |
| 9 | 标签体系 | 纯受控词表 / 受控+自由标签 / 完全自由 | **受控词表 + 允许自由标签** |
| 10 | 用户端可见性 | 完全静默只看开关 / 可见只读 / 可见可纠错 | **完全静默，只有开关** |
| 11 | 管理端位置 | 新一级菜单 / 挂在用户管理里 / Tab | **挂在"用户管理"板块里** |
| 12 | 人工编辑与模型重写共存 | 锁定 / 人工优先 / 不锁定可能被覆盖 | **不做锁定，即时生效，下次重写覆盖** |

另外两条默认值（我们这么定的，可调）：
- 变更痕迹：**只留最新一份**，不存历史快照。
- 回灌开关语义：**只关"回灌"，不关"累积"**（关掉后后台仍沉淀，只是不灌进 prompt）。这一条如果需求方要的是"关掉就完全停止记录"，改动点只在回灌构建函数一处。

---

## 二、实施步骤（按依赖顺序执行）

### Step 1 · 数据层：建表与改表

**新建画像主表**（一个用户一行，user_id 唯一）：
- 受控偏好标签及权重（JSON 字符串，形如 `[{"tag":"自然风光","weight":3}]`）
- 自由标签（JSON 数组，仅展示，不参与筛选聚合）
- 硬性约束（文本，分号分隔）
- 固定字段：预算档位 / 节奏 / 同行人 / 偏好天数
- AI 文字摘要 + 摘要最后生成时间
- 累计对话轮数（用于判断是否值得重写）
- 最后更新来源（model / manual，用于管理端区分人与机器）
- 回灌开关（默认开）
- 运营备注 + 运营标记
- create_time / update_time（update_time 建议 on update）

**改造消息表**：加 `user_id` 列 + 索引。存量历史消息保持 NULL，**不做归属回溯**（无法可靠判定归属）。

**同步产出**：版本化迁移脚本（如果没有迁移工具，就按项目现有习惯命名并给出可直接执行的 SQL），同时按项目部署习惯再产一份部署用 DDL。

> 坑：如果项目已有旧的"会话表"设计（比如只建了表没写代码的遗留表），不要顺手去用它，先确认真实在跑的是哪张表，改动要落在真正生效的那张上。

### Step 2 · 领域层：实体 / DTO / VO

- 画像实体（字段与表一一对应）
- 标签对象（tag + weight）——**单独建类，不要用裸 Map**，否则序列化字段顺序不可控，后面按标签做 JSON 子串筛选会不稳定
- 管理端画像详情 VO（列表与详情可共用）
- 人工编辑入参 DTO（**只允许改画像内容，不允许改 user_id 和回灌开关**）
- 运营备注入参 DTO（备注+标记）
- 用户端开关入参/出参 DTO

### Step 3 · 受控词表与规则抽取器

**这一步是整个功能"确定性"的来源，必须把它当成一个独立常量模块写，不要散落在 service 里。**

内容：
1. **受控标签词表**（与前端已有的偏好选项保持一致，不要另创一套）。我们用的 9 个：经典必玩 / 吃吃喝喝 / 小众探索 / 拍照出片 / citywalk / 自然风光 / 历史古建 / 逛街购物 / 文艺展览。
2. **标签 → 命中关键词**的映射表（每个标签给 5~10 个关键词）。
3. **预算档位 / 节奏 / 同行人**三组取值范围 + 各自的关键词映射。
4. **硬性约束关键词**表（不吃辣、忌口、过敏、不爬山、腿脚不便…）。
5. 工具方法：是否为受控标签、各维度取值全集（供模型输出校验与前端下拉）。

抽取规则要写清优先级与去重：
- **先吃结构化字段（问卷/表单里已有的天数、预算、人数、已有偏好标签），再用文本关键词补充**。结构化字段比文本挖掘可靠得多。
- **同一轮对话内同一标签只加 1**（用 Set 去重），否则结构化字段+文本会重复计数。
- 关键词要避免过短导致误命中（比如"山"会命中"山东""山城"，应换成"爬山/山水"）。
- 同组关键词之间不要有包含关系（比如"预算充足"同时属于两个档位，会让档位判断不稳定）。

### Step 4 · 独立模型客户端（摘要模型）

- 走 **OpenAI 兼容格式**的 `/chat/completions` 最通用，任何兼容厂商只改 base_url/key/model。
- 用**独立的配置项**（url / api-key / model 三个），与对话、规划用的模型完全分开。
- **三项任一为空 → 直接返回 null 并打 warn 日志，不抛异常**：这是降级的设计核心，保证缺配置时主链路照常跑。
- 调用异常（网络、401、模型不存在）一律 catch 住返回 null，**不要向上抛**，因为这是在业务成功之后跑的旁路任务。
- 返回内容宽松解析：剥掉 markdown 代码块、容忍前后有解释文字（取第一个 `{` 到最后一个 `}`）；**解析失败时退化成"把整段文本当摘要保存"**，不要丢弃。

### Step 5 · 写入链路（对话时）

在对话服务里按顺序做四件事：
1. 从登录上下文取 userId。
2. **调用 AI 之前**先构建并注入画像回灌文本（见 Step 7）。
3. 消息落库时带上 user_id（本轮 user + assistant 两条）。
4. 落库后调用**规则累加**（见 Step 6），用 try/catch 包住——**画像任何失败都不能让用户对话失败**。

注意顺序：如果项目原本是"先调模型、再落库"，画像注入必须插在调模型之前；画像累加放在最后。

### Step 6 · 规则累加的具体动作（每轮对话）

一次累加做这些事（全部同步、无模型调用、足够便宜）：
1. 取或建画像行（**注意并发首轮可能同时插入，要处理唯一键冲突后回查**）。
2. 结构化字段：把请求里已有的爱好标签计入本轮命中；天数直接覆盖；预算字符串解析出金额→映射档位；人数文本→映射同行人。
3. 文本抽取：用户输入里命中关键词的受控标签计入本轮命中；文本命中预算/节奏/同行人关键词则覆盖对应字段；命中约束关键词则**追加**（不是覆盖）到约束字段，按分号去重合并。
4. 命中标签权重各 +1，按权重倒序、同权重按词表顺序稳定排序后写回 JSON。
5. 累计对话轮数 +1。
6. 更新来源**不要**在规则累加时改写（来源只表示"画像内容最后一次是谁写的"，留给模型重写和人工编辑去标）。

预算金额→档位建议直接对齐问卷档位（保证同一套语义），例如：≤500 经济 / ≤2000 适中 / >2000 高档。

### Step 7 · 回灌链路

**构建回灌文本**（供对话和规划共用）：
- 开关关闭 → 返回 null；无画像 → 返回 null。
- 内容按这个顺序拼：高频偏好（取权重最高的前 5 个）、其他已知特点（自由标签）、结构化偏好（预算/节奏/同行人/天数）、硬性约束、画像摘要。
- 外层包一句明确指令，例如："【该用户的历史画像（来自以往对话，可直接参考，不要重复追问已知信息）】"。
- 整个构建过程 try/catch，失败返回 null，不能影响对话。

**注入位置**：写进请求体里透传给 AI 的那个基础信息对象的一个新字段（我们叫 `profile_note`）。
- 这个字段**只由后端服务端注入，前端不传**。
- AI 服务侧的入参模型要显式声明这个可选字段（很多框架会静默丢弃未声明的额外字段——**这一点必须验证**，否则前端看起来接了、实际全丢）。

**必须在请求线程内注入**：如果行程生成是异步任务（提交后丢线程池），后台线程拿不到登录上下文（ThreadLocal 取不到用户）。所以注入动作要在提交任务的那个线程里完成。

**缓存必须加画像指纹**（如果项目有按"城市+天数+偏好+预算"缓存行程结果）：
- 加回灌后，同样的参数在不同画像下应该产出不同结果。指纹不进 key 的话，缓存命中会让回灌**静默失效**。
- 做法：有回灌时在 key 尾部追加画像文本的短哈希；无回灌时保持原 key（兼容历史缓存）。

### Step 8 · AI 侧 prompt 接入

- 请求入参模型加可选字段（Step 7 提到的）。
- 各 prompt 模板里加一句说明：该字段非空时是用户历史画像，请直接沿用其中已知偏好与硬性约束，不要重复追问；为空则按新用户处理。
- 我们在对话 prompt 和三个规划 prompt（完整/框架/详情）都加了这句。
- 因为基础信息对象本身是以 JSON 文本塞进 prompt 的，新字段会自动出现；加说明句是为了提高模型的实际遵守率。

### Step 9 · 触发链路（业务成功时）

在"生成行程成功"落库的那一步之后触发摘要重写：
- 只在**本条业务数据携带 AI 会话 id** 时触发（没会话说明不是 AI 对话产出的，不该沉淀）。
- 触发方式：丢进**独立的线程池**（与行程生成线程池分开，避免长任务互相排队），线程名带上业务前缀便于排查。
- 异步任务内部：查画像行（没有就跳过）→ 拉该会话的对话记录 → **只取最近 N 条、单条截断**（控制 token）→ 调摘要模型 → 宽松解析 → 写回。
- 写回内容：摘要文本、自由标签（过滤掉受控标签，避免和规则维护的权重打架）、约束、三个固定字段（校验取值必须在词表内才写）、偏好天数（>0 才写）、更新来源标记为 model、摘要生成时间。
- **受控标签权重不要动**（那是规则的职责，模型改会把"命中次数"语义毁掉）。
- 失败只打 warn，任务已成功返回给用户了，不能让旁路失败影响体验。

**摘要的 JSON 契约**（明确写进 prompt，要求只输出 JSON、不要解释、不要代码块包裹）：
```
{
  "summary_text": "≤200 字画像摘要",
  "free_tags": ["自由标签"],
  "constraints": "硬性约束，无则空字符串",
  "budget_level": "经济/适中/高档 之一，判断不了填空串",
  "pace": "悠闲/常规/暴走 之一",
  "companions": "独自/情侣/朋友/家庭 之一",
  "prefer_days": 整数，判断不了填 0
}
```
并在 prompt 里补一句：已有画像中已确认的信息（如约束）除非本次明确推翻，否则原样保留。

### Step 10 · 鉴权改造（如果原来对话接口是匿名的）

- **前后端都要拦**：后端把"对话/生成计划/查历史"这三个接口从匿名白名单里移出；前端在进入对话页/发消息前判断登录态并跳登录（带 redirect 回跳）。
- **只改需要的接口**：白名单是整段路径排除的话，要改成精确列出仍可匿名的接口（我们保留了状态轮询、取消任务、旧表单式生成），把影响面压到最小。
- 顺手检查：**用户端拦截器有没有在请求结束时清理 ThreadLocal**。很多项目只有管理端拦截器做了清理。一旦对话依赖登录上下文，不清理会在 Tomcat 线程复用时读到上一个请求残留的 userId——这是个会串数据的隐患，必须补。
- 前端的请求封装里，这三个接口要**去掉 skipAuth 标记**，否则后端拦了、前端不带 token，用户只会看到 401。

### Step 11 · 管理端接口

放在已有"用户管理"前缀下的独立子路径（例如 `/admin/user-profile/**`），**不要挤在 `/admin/user/{id}` 这种通配路由旁边**，避免路径匹配歧义。

接口清单：
| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/admin/user-profile/{userId}` | 画像详情（**无画像时返回带用户基础信息的空结构**，不要 404） |
| GET | `/admin/user-profile/tags` | 受控标签词表（筛选下拉用） |
| GET | `/admin/user-profile/export` | 按当前筛选条件导出（不分页、有上限，前端转 CSV） |
| PUT | `/admin/user-profile/{userId}` | 人工修正画像（**整份覆盖，允许清空**） |
| PUT | `/admin/user-profile/{userId}/remark` | 更新运营备注与标记 |

同时改造**用户列表接口**：
- 入参加画像标签筛选、画像标记筛选。
- 实现方式：先按画像条件查出命中的 user_id 集合，**为空就直接返回空页**（不要退化成全量查询），再收敛用户分页查询；拿到当页用户后**批量**补画像字段（一次查完，不要在循环里查库）。
- 标签筛选用 JSON 子串匹配即可（受控标签不含引号转义），用**带引号的标签名**做子串，这样与 JSON 字段顺序无关；注意参数化，别拼字符串。
- 返回的列表项里带：标签+权重、摘要、对话轮数、画像更新时间、来源、备注、标记。没有画像的用户，标签置空集合。

**权限点**：优先**复用**已有的查看/管理权限点（我们复用了 `user:view` / `user:manage`）。新增权限点意味着还要去改角色表里的权限串，否则菜单不显示、页面被路由守卫重定向——这类"代码没问题但看不到"的坑很容易误判成功能没生效。

### Step 12 · 管理端页面

在"用户管理"页做增量，不新建页面：
1. 筛选栏加：画像标签下拉（选项来自 Step 11 的词表接口）、画像标记输入、导出按钮。
2. 列表加"AI 画像"列：显示前 3 个标签（带权重）+ 摘要两行截断；无画像显示"暂无画像"。
3. 加"画像"按钮打开抽屉，抽屉内容分区：
   - 基础信息（用户/手机号/对话轮数/回灌开关状态/画像更新时间/更新来源）
   - 受控偏好标签（查看态显示标签+权重；编辑态是"标签下拉 + 权重数字输入 + 删除"的行列表 + 添加按钮）
   - 结构化偏好（查看态用描述列表；编辑态用四个下拉/数字输入，可清空）
   - 自由标签（编辑态用多行文本，一行一个）
   - 硬性约束、AI 画像摘要
   - 运营备注/标记（**独立保存按钮，不要和画像编辑一起提交**）
   - 底部：编辑画像 / 保存画像 / 取消，并放一句提示"人工修改即时生效，用户下次生成行程时会被 AI 重写覆盖"（让运营知道这是有意的）
4. 保存画像后**重新拉取详情**再渲染（以服务端返回为准，别用前端本地拼的值）。
5. 导出：前端把导出接口的结果拼成 CSV，**带 BOM** 否则 Excel 打开中文乱码；字段值用双引号包裹并把内部引号转义。

### Step 13 · 用户端开关

- 后端：查询/更新回灌开关两个接口，挂在"用户信息"前缀下（天然要求登录）。
- 前端：放在**对话页**（我们放在输入框上方一行，开关缩放到与文字同高），默认开启。
- 交互：**先乐观更新 UI，失败回滚**，成功给个轻提示；读取失败保持默认开启、不打断对话。
- 语义提醒：这个开关是用户端对画像**唯一**的可见入口，文案建议是"使用 AI 记住的偏好"这种从用户视角出发的说法，不要出现"画像"这种内部词。

---

## 三、必须避开的坑（逐条都是会真出问题的）

1. **AI 服务侧入参模型没声明新字段 → 静默丢弃**。前端/后端看着都传了，实际 prompt 里什么都没有。改完必须验证字段真的到了模型侧。
2. **异步任务里取登录上下文**。后台线程的 ThreadLocal 是空的，画像注入必须发生在提交任务的请求线程内。
3. **缓存 key 不含画像指纹** → 回灌静默失效（这是最隐蔽的一个，功能"看起来做好了"但经常不生效）。
4. **消息表原来没有 user_id 却要求"按用户聚合画像"** → 必须先确认表结构再动手，不要假设有。
5. **用户端拦截器不清理 ThreadLocal** → 线程复用串用户数据。
6. **匿名白名单整段排除**（如 `/travel/**`）→ 改成精确列出，明确哪些仍匿名，把影响面写进报告。
7. **新增管理端权限点却不更新角色权限串** → 菜单不显示、路由被重定向，误判为代码没生效。
8. **标签 JSON 用裸 Map 序列化** → 字段顺序不确定，按标签做子串筛选会时灵时不灵。用固定字段顺序的类，并用带引号的标签名匹配。
9. **关键词过短或互相包含** → 误命中和档位抖动（""山"命中"山东"、"预算充足"同时属于两个档位）。
10. **同轮内结构化字段与文本抽取重复计数** → 用 Set 按轮去重。
11. **并发首轮插入画像行** → 唯一键冲突，要 catch 后回查。
12. **模型重写时改受控标签权重** → 与规则累加互相覆盖，权重失去意义。
13. **摘要在同步链路里调模型** → 用户要等模型；必须异步，且失败不冒泡。
14. **旁路任务抛异常影响主链路** → 规则累加、回灌构建、摘要生成三处全部 try/catch + warn 日志。
15. **管理端导出 CSV 不带 BOM** → Excel 中文乱码。
16. **人工编辑语义没和需求方对齐** → "不做锁定"意味着运营改完下次就被模型冲掉。要么接受并写进页面提示，要么改成锁定，别让它保持模糊。

---

## 四、验收清单（按这四类跑，不要只跑 happy path）

**A. 端到端主链路**（一条链跑通，逐点断言）
1. 未登录调对话接口 → 401；未登录调历史 → 401。
2. 登录 → 对话 1 轮 → 断言：消息表本轮两条都带 user_id；画像行已建；结构化字段与文本命中的标签权重都 +1；约束已记录；预算/节奏/同行人/天数按预期映射。
3. 再对话 1 轮 → 断言：目标标签权重 +1（不是重复覆盖）、累计轮数递增。
4. 生成行程 → 断言：任务最终 DONE。
5. 生成过程中读日志，断言：**缓存 key 带上了画像指纹**（这是回灌真的生效的证据）。
6. 保存行程成功 → 轮询等待 → 断言：AI 摘要已写入、更新来源为 model。
7. 管理端登录 → 读画像详情 → 断言：标签有序、字段完整、摘要非空。
8. 按画像标签筛选用户列表 → 断言命中该用户。
9. 人工编辑画像 → 断言落库且来源为 manual。
10. 更新备注/标记 → 断言落库。
11. 按标签导出 → 断言命中。
12. 管理端接口未登录 → 401。
13. 用户关闭回灌开关 → 断言库中为关；再次生成行程时**缓存 key 不再带指纹**（证明回灌真的停用）。

**B. 影响面回归**
- 原先匿名的接口仍然匿名（旧表单式生成、首页接口等）。
- 原先需要登录的接口行为不变。
- 只新增了一个"对话链路需登录"的行为变更，逐条列出并确认。

**C. 降级与异常**
- 摘要模型**三项配置为空**：对话与保存行程照常成功、标签照常累加、摘要保持为空、日志有"未配置跳过"warn。
- 摘要模型**调用失败**（用一个不存在的模型名）：表现同上，日志有调用失败 warn。
- 断言失败路径**没有写入半成品摘要**、没有把来源标成 model。
- 人工编辑 → 再次触发重写 → 断言被覆盖（这是"不做锁定"的预期行为，要显式验证而不是当 bug）。

**D. 管理端 GUI 验收（有浏览器自动化就跑）**
- 登录 → 进用户管理 → 列表有数据。
- 标签下拉能加载出完整词表（断言数量）。
- 选标签筛选 → 列表有命中，画像列展示标签+权重。
- 点画像 → 抽屉渲染出基础信息/标签/固定字段/自由标签/约束/摘要/备注区。
- 点编辑 → 标签行与权重输入渲染正常。
- 改内容保存 → 出现成功提示，且**回读到服务端最新值**。
- 保存备注 → 无应用报错。
- 导出 → 触发 CSV 下载。
- 全程收集控制台错误与 4xx/5xx 响应，断言无应用级报错（favicon 404 之类要单独识别并排除，否则会一直假失败）。

---

## 五、交付时要一起说明的事

1. **上线前必须执行的 SQL 清单**（建表 + 改表），以及项目部署用的那份 DDL 同步情况。
2. **模型配置项**（url / key / model），以及不填时会怎样（降级行为）。
3. **本次唯一的产品行为变更**：对话链路从匿名变为必须登录，对游客体验的影响要显式告知。
4. **顺带发现的环境漂移**：比如本地库里缺失的、代码已经在用的历史迁移列。这类问题会以完全无关的报错形式挡住主链路，必须提前确认生产库已执行。
5. **既有脆弱点**（如 AI 调用读超时 vs 实际耗时的余量），标明不是本次引入，但给出观察到的实测数字。
6. **测试数据与临时脚本的去留**，并给出清理 SQL。
