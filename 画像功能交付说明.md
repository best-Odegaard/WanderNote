# AI 用户画像功能 · 交付说明

> 对应文档：`画像功能实施操作说明书.md`｜交付日期：2026-09-14｜分支状态：未提交（工作区改动）

---

## 〇、一句话结论

按说明书把「对话沉淀 → 管理端板块」整套做完了：**12 个决策点全部按"我们的选择"落地**，
后端 + 用户端 + 管理端 + Python agent 四处都改了，编译/打包/构建全部通过，
并在**真实库 + 真实服务**上跑了验收（含管理端 GUI），数据库已还原到验收前的状态。

**上线前只需要确认一件事**：生产库里 `user_profile` 表的列名（见第一节）。
本次开发库里这张表**已经存在**，且列名与文档描述不同，代码是按库里实际列名写的。

---

## 一、上线前必须执行的 SQL 清单

| # | 脚本 | 动作 | 开发库现状 | 生产库要做什么 |
|---|---|---|---|---|
| 1 | `部署表结构/V2.1__create_user_profile.sql` | 建 `user_profile` 主表 | **已存在**（2026-09-14 15:58 由更早的一版建过，含 3 行测试数据） | 先跑自查 SQL 确认表在不在；不在就执行建表，在就**跳过** |
| 2 | `部署表结构/V2.2__alter_chat_history_add_user_id.sql` | `chat_history` 加 `user_id` + 索引 | **列和索引都已存在** | 同上：先自查，缺才加；不缺跳过 |

两个脚本头部都写了可直接粘贴的自查 SQL。`V2.1` 是 `create table if not exists`，重复执行安全；
`V2.2` 是 MySQL 的一次性 DDL，在有 `user_id` 的库上重复执行会报 `Duplicate column name 'user_id'`，属预期。

### ⚠️ 列名以库为准，不要按文档改名

文档只规定了字段语义，没规定列名。开发库里 `user_profile` 实际列名是：

```
id, user_id, pref_tags, free_tags, constraints, budget_level, pace, companions,
prefer_days, summary_text, summary_update_time, chat_round_count, last_source,
use_preference, remark, mark, create_time, update_time
                                        ↑ 唯一键 uk_user_profile_user_id(user_id)
```

`pref_tags / constraints / chat_round_count / last_source / use_preference / mark` 这几个名字和"直觉名字"不一样，
所以 Java 侧用 `@TableField` 集中做了映射（`com.gkv.entity.UserProfile`），**没有去改库里的列名**——
这张表不是我建的、库里已经有数据，改名属于无谓的环境变更。

**如果谁按别的列名建表**，`create table if not exists` 会静默跳过，代码里的列名一个都对不上 →
运行时报 `Unknown column`，而画像链路整条包在 try/catch 里（旁路不允许影响对话），
表现是"功能看起来做好了但一直不生效"。这是最难查的一类故障，所以专门写在这里。

> 已在开发库上做过的核对：把实体的 18 个字段（ChatHistory 6 个）与 `information_schema.columns`
> 逐列比对，**完全一致**。

### DDL 同步情况

- 版本化迁移脚本：`部署表结构/V2.1__*.sql`、`V2.2__*.sql`（沿用项目 `V1.9__ / V2.0__` 的命名习惯）。
- 部署用 DDL：本项目的部署习惯就是「`部署表结构/` 下的版本化脚本 + `deploy/部署说明.md` 的建表清单」，
  两者已同步（`部署说明.md` 新增了 1.1 / 1.2 两节）。
- `部署表结构/trip_plan_geek012.sql` 等旧快照**没有改**：它们已经与线上库漂移（见第四节），
  本次不去动它们，只在部署说明里记明差异。

---

## 二、模型配置项（**可以先不配就上线**）

`application.yml` 新增三项，走 OpenAI 兼容的 `/chat/completions`，与对话/规划用的模型**完全分开**
（对话/规划的模型在 Python agent 的 `travel_self_agent/agent/travel_agent.py` 里硬编码）：

```yaml
profile:
  summary:
    url: ${PROFILE_SUMMARY_URL:}        # 到 /chat/completions 为止，不是 base_url
    api-key: ${PROFILE_SUMMARY_API_KEY:}
    model: ${PROFILE_SUMMARY_MODEL:}
```

**三项任一为空 → 摘要重写直接跳过并打一行 warn，其余功能完全不受影响**
（对话照常、生成行程照常、标签权重照常累加，只是没有 AI 文字摘要）。

调用异常（网络、401、模型不存在）也一律 catch 住返回 null，不向上抛——这是业务成功之后跑的旁路任务。
日志关键字：`ProfileSummary` / `ProfileRewrite`；未配置时会看到
「摘要模型未配置（profile.summary.url/api-key/model 存在空值），跳过摘要重写」。

---

## 三、本次唯一的产品行为变更：对话链路从匿名变为必须登录

**这是本次唯一对游客可见的行为变化，需要显式告知。**

| 接口 | 变更前 | 变更后 |
|---|---|---|
| `POST /travel/chat` | 匿名 | **必须登录**（401） |
| `POST /travel/generatePlan` | 匿名 | **必须登录**（401） |
| `GET /travel/history` | 匿名 | **必须登录**（401） |
| `POST /travel/aiPlan`（旧表单式生成） | 匿名 | 仍匿名 |
| `GET /travel/plan/status/{taskId}` | 匿名 | 仍匿名 |
| `POST /travel/plan/cancel/{taskId}` | 匿名 | 仍匿名 |
| `/ai/**`、`/home/**` | 匿名 | 仍匿名 |

**为什么必须改**：画像要按 `user_id` 聚合，而 `chat_history.user_id` 只能从登录上下文取。

**白名单改法**：原来是「整段 `/travel/**` 排除」，现在改成**精确列出仍可匿名的三个路径**，
把影响面压到最小（这三条前端本来就在进入对话页之前/后台轮询时调用，见
`WebMvcConfiguration.addInterceptors`）。

**对游客的影响**：未登录进对话页 → 提示「请先登录」并跳登录页（带 `redirect` 回跳），登录后自动回到对话页。
前端已同步：这三个接口去掉了 `skipAuth`（否则后端拦了、前端不带 token，用户只会看到 401）。

**顺带修掉的一个串数据隐患**：`JwtTokenUserInterceptor` 原来**没有清理 ThreadLocal**
（只有管理端拦截器清理了）。Tomcat 复用工作线程时，任何「不经过该拦截器」的请求落到刚跑过登录用户请求的线程上，
`BaseContext` 里还留着上一个请求的 userId，`AutoFillAspect` 读到的就是别人的身份。已补 `afterCompletion` 清理。

---

## 四、顺带发现的环境漂移（上线前必须确认生产库已执行）

这类问题会以**完全无关的报错**形式挡住主链路，所以单独列出来。

> **❗ 修正说明**：本节第一版只写了 `chat_history` / `trip_plan.chat_session_id` 两处，
> **低估了范围**。本机拉起环境做验证时又被两个缺失的迁移挡住（`/trip/list` 直接 500、
> `/featured/list` 报表不存在），所以这里补全，并且**在开发库上把缺的两个迁移都执行了**。

| 发现 | 证据 | 影响 | 本次处理 |
|---|---|---|---|
| **`trip_plan` 缺 `start_date` / `end_date` 两列** | `V1.9__alter_trip_plan_add_dates.sql` **未在开发库执行**；实体 `TripPlan` / `TripPlanVO` 已在用这两个字段 | `GET /trip/list`、`GET /trip/{id}` 直接 **500**：`Unknown column 'start_date' in 'field list'`。登录用户一打开首页就报错 | **已在开发库执行 V1.9**，接口恢复 |
| **`featured_trip` 表不存在** | `V2.0__create_featured_trip.sql` **未在开发库执行** | `GET /featured/list` 报 `Table 'geek012.featured_trip' doesn't exist`，首页轮播 500 | **已在开发库执行 V2.0**（表已建，内容为空） |
| `chat_history` 实际是 `role varchar(20)` / `content longtext` | 实查 `information_schema` | 快照 `部署表结构/chat_history.sql` 写的是 `varchar(16)` / `text`，**快照已过期** | 未改快照，仅记录 |
| `trip_plan` 实际有 `chat_session_id` 列 | 实查 + 实体 `TripPlan.chatSessionId` 在用 | 快照 `trip_plan_geek012.sql` 没声明这一列 | 未改快照，仅记录 |
| 库名在快照里不统一 | `chat_history.sql` 写 `aitravel.*`，`V1.9/V2.0` 写 `geek012.*` | 执行前必须换成服务器实际库名 | 仅记录 |
| 应用用户表是 **`sys_user`** | `User` 实体 `@TableName("sys_user")`；`geek012.user` 不存在 | 按 `部署表结构/user.sql` 里的 `user` 找会找不到表 | 仅记录 |
| `ai_chat_session` / `ai_chat_message` 是**没有代码在用的遗留表** | 全仓 grep 只有前端一个 localStorage 键名 `ai_chat_session_id` 撞名 | 真正生效的对话表是 `chat_history`（走 `ChatHistoryMapper`）。**本次没有使用那两张遗留表** | 仅记录 |
| `context_note` 传了但没生效 | 见下 | `部署说明.md` 4.1 节原来声称它"是 agent 专门的外部上下文入口"，**该说法不成立** | 已在 `部署说明.md` 里更正 |

**系统性核对过一遍**：把 18 个实体 `@TableName` 与库里真实表做 diff，
现在缺 0 张（之前缺 `featured_trip`）；库里多出的 `ai_chat_session` / `ai_chat_message` /
`travel_journal_browse` 都没有实体在用（后一个是浏览量记录，属正常）。

库存量数据核实：`user_profile` 有 3 行（更早一版的自动化验收数据）、
`trip_plan` 有 1 行（id=10，三亚行程）、`sys_user` 有 8 个账号。

### `context_note` 的真相（本次实测）

Python 侧 `main.py` 的 `BaseInfo` **没有声明 `context_note`**，而 pydantic v2 默认 `extra='ignore'` +
序列化走 `model_dump()`，所以后端传的 `context_note` **在进 prompt 之前就被静默丢掉了**。
链接导入之所以看起来正常，是因为同一段正文被复制放进了 `user_input` / `chat_history`（这两个能到 prompt）。

本次的 `profile_note` 已经在 `BaseInfo` 里**显式声明**，并实测确认能进 prompt（见第七节 G）。

---

## 五、既有脆弱点（**不是本次引入**，附实测/代码数字）

1. **前端生成轮询超时 < 后端实际生成耗时**
   - 前端：`frontend/src/api/trip.ts` 里 `deadline = Date.now() + 4 * 60 * 1000` → **240 秒**
   - 后端：`RestTemplate` readTimeout = **900 秒**（`WebMvcConfiguration`）
   - 部署说明记录的既有实测值：**一个多日行程 3~8 分钟（180~480 秒）**
   - ⇒ 落在 **240~480 秒**之间的生成，会被前端提前判成「生成超时，请稍后重试」，
     而后端其实还在跑、并且**跑完会把结果写进缓存**。用户重试一次反而可能秒开（缓存命中）。
     本次没有跑真实生成（agent 未运行），所以**没有实测本次的生成耗时**，上面数字都来自代码常量与既有记录。
2. **`PlanTaskService.tasks` 是无上限 `ConcurrentHashMap`**，30 分钟 TTL 只在**被轮询到那个 taskId 时**才惰性清理
   → 提交后不再轮询的任务永不释放。
3. **agent 是单进程同步服务，且与 DXY 共用同一个进程**（部署说明记载），忙时排队；
   叠加 readTimeout 余量后，高峰期更容易撞到超时。
4. **行程缓存 key 仍不含 `departure_city` 与出发日期**（`TravelPlanCacheService.buildKey`）。
   本次只加了**画像指纹**：不同画像的人不再互相命中；但**同一画像下**不同出发地/日期的请求仍共用缓存。
5. **向导默认预算 3000**（`frontend/src/pages/plan/wizard.vue`）→ 只要用户不动预算字段，
   结构化预算就是 `"3000"` → 规则按"结构化字段优先"会判成**「高档」**。
   画像开始消费这个字段之后，这一点的数据质量影响才显出来，建议要么调默认值、要么在向导里强制选择。

---

## 六、测试数据与临时脚本的去留

### 临时脚本：全部已删除

- 规则引擎自检（38 项断言）：写在系统临时目录，**不在仓库里**，跑完已删。
- `profile_note` 链路校验（12 项断言）：临时拷进 `travel_self_agent/` 跑完即删。
- 数据库还原脚本：临时目录，跑完已删。

### 数据库：已还原到验收前状态

验收过程中改过开发库，**全部已还原**，并逐字段比对确认与验收前**完全一致**：

| 动作 | 处理 |
|---|---|
| 用户端开关验收时新建的 `user_profile(user_id=3)`（rogers） | 已 DELETE（测试前并不存在） |
| 管理端人工修正验收改动的 `user_profile(id=4)` | 已 UPDATE 回原值（含 `update_time`） |
| 管理端 GUI 验收把标签权重改成 10 | 已改回 9 |

### 库里的既有测试数据：**没有动**（不是本次产生的）

`user_profile` 里有 3 行、`sys_user` 里有 3 个 `e2e_*` 测试用户，都是**今天 15:58~16:39 更早一版留下的**
自动化验收数据（`e2e_profile_u1` / `e2e_deg_cf` / `e2e_deg_nc`，备注写着"UI 自动化写入的…"）。
我不去删别人留下的数据，需要清理的话用下面这段：

```sql
-- 清理更早一版留下的画像验收数据（谨慎：确认这些 user_id 确实是测试账号再执行）
DELETE FROM geek012.user_profile WHERE user_id IN (
    SELECT id FROM geek012.sys_user WHERE username LIKE 'e2e\_%'
);

-- 清掉本次画像链路产生的开发库会话消息（保留老会话）：按需
-- DELETE FROM geek012.chat_history WHERE user_id IS NOT NULL;
```

---

## 七、验收记录（实际跑过什么、结果如何）

### A. 编译 / 构建

| 项 | 结果 |
|---|---|
| 后端 `mvn compile` / `mvn package` | 通过（jar 98.9MB） |
| Spring 上下文启动 | 通过（新增 Bean 装配、依赖注入无误） |
| `admin-web` `npm run build` | 通过 |
| `admin-web` `vue-tsc --noEmit` | 本次改动的文件 0 报错（`AdminLayout.vue` 有 7 条**既有**报错，与本次无关） |
| 用户端 `npm run build:h5`（uni-app） | 通过；`vue-tsc` 在本次改动文件上 0 报错（其余 37 条为既有报错） |

### B. 规则引擎自检（38 项断言，全过）

覆盖说明书点名的那几个坑：

- 受控词表 9 个标签、每个 5~10 个关键词、**同组关键词无互相包含**、**不同维度关键词无跨组冲突**
- `"山东/山城"` **不**误命中任何标签（短词陷阱）
- `"预算充足"` 只落在高档、不跨档位
- `"女朋友"` 判成**情侣**而不是朋友（跨组包含靠声明顺序 + 首次命中即停）
- 同轮内同一标签**只记 1 次**（Set 去重，结构化字段 + 文本不重复计数）
- 结构化优先 / 文本覆盖 / 天数直接覆盖 / 约束合并去重 / 金额解析（`人均1000`→1000、`500元以内`→500）
- **正文里的数字不会被当金额**（避免"我们有3个人"被误判成经济档）
- 标签 JSON **字段顺序固定为 `tag,weight`**、权重倒序、同权重按词表顺序（筛选稳定性）
- 回读真实库里的画像（`[{"tag":"自然风光","weight":9},…]`）正确；脏数据不抛异常
- 回灌文本：开关开→有内容且含指令头；**开关关→null**（只关回灌不关累积）；无画像→null
- 画像指纹随文本变化、8 位

> 修掉一个自查发现的**真 bug**：预算档位原来写了关键词 `"适中"`、节奏里写了 `"适中节奏"`，
> 一句"适中节奏"会同时命中两个维度、让档位跟着节奏抖。已把预算关键词全部改成带"预算"语境
> （`预算适中 / 中等预算 / 预算一般`），并把这条不变量写进了自检。

### C. 鉴权边界（实测 HTTP 状态码）

| 请求（无 token） | 结果 | 期望 |
|---|---|---|
| `POST /travel/chat` | **401** | 401 ✅ |
| `GET /travel/history` | **401** | 401 ✅ |
| `POST /travel/generatePlan` | **401** | 401 ✅ |
| `GET /travel/plan/status/x` | 200 | 仍匿名 ✅ |
| `POST /travel/aiPlan` | 200 | 仍匿名 ✅ |
| `GET /admin/user-profile/8` | **401** | 401 ✅ |
| `PUT /user/profile/switch` | **401** | 401 ✅ |

### D. 用户端开关（实测）

- `GET /user/profile/switch` → `{"enabled":true}`（**没有画像行时默认开启**）
- `PUT /user/profile/switch {"enabled":false}` → `{"enabled":false}`，且**库里落成 `use_preference=0`** ✅
- 越权读别人的会话：用 rogers 的 token 读 user 8 的 sessionId → 返回 `[]`（归属校验生效）✅

### E. 管理端接口（真实库、真实 HTTP）

| 用例 | 结果 |
|---|---|
| `GET /admin/user-profile/tags` | 返回 9 个受控标签，顺序正确 ✅ |
| `GET /admin/user-profile/8` | 标签带权重、自由标签、约束、固定字段、摘要、`lastUpdateSource=manual`、`injectEnabled=0` 全部正确读出 ✅（**反向验证了 `pref_tags/constraints/chat_round_count/last_source/use_preference/mark` 这几个 @TableField 映射**） |
| `GET /admin/user?profileTag=自然风光` | `total=1`，只命中 user 8，且画像字段批量补齐 ✅ |
| `GET /admin/user?profileTag=文艺展览`（不存在的标签） | `total=0`、`records=0`——**没有退化成全量查询** ✅（对比：无筛选时 total=8） |
| `GET /admin/user?profileMark=GUI` | `total=1` ✅ |
| `GET /admin/user/export?profileTag=自然风光` | 1 行，含全部画像字段 ✅ |
| `PUT /admin/user-profile/8`（整份覆盖 + 清空） | 见下 ✅ |

**人工修正的"允许清空"实测**（这是本次最容易做错的一点，因为 MyBatis-Plus 默认
`update-strategy=NOT_NULL` 会跳过 null 字段、根本清不掉）：

```
提交：budgetLevel="超级贵"（词表外）, pace="", companions="", preferDays=null,
      summaryText="", constraintsText="", freeTags=[], tags=[吃吃喝喝, 非受控乱写]
落库：constraints=NULL  budget_level=NULL  pace=NULL  companions=NULL
      prefer_days=NULL  summary_text=NULL
      pref_tags=[{"tag":"吃吃喝喝","weight":5}]   ← 非受控标签被丢弃
      last_source=manual
      未变：chat_round_count=2  use_preference=0  （规则维护的轮数 & 用户自己的开关）
```

服务端 SQL 日志实证（SET 列表里**没有** `user_id` / `use_preference` / `chat_round_count`）：

```sql
UPDATE user_profile SET pref_tags=?,free_tags=?,constraints=?,budget_level=?,pace=?,
       companions=?,prefer_days=?,summary_text=?,last_source=?,update_time=? WHERE (id = ?)
UPDATE user_profile SET remark=?,mark=?,update_time=? WHERE (id = ?)   -- 备注独立保存
```

**顺带修的一处一致性问题**：清空摘要时，`summary_update_time` 原来会留成一个没有摘要的时间戳，
现在会一起清掉（对应地，人工写摘要时不改这个时间——它记的是"最后一次 AI 生成时间"，
"人来改过"由 `last_source=manual` 表达）。

### F. 管理端 GUI 验收（浏览器自动化，全过）

用真实浏览器走了一遍：登录 → 用户管理 → 筛选 → 抽屉 → 编辑保存 → 备注 → 导出。

- 页面渲染出新增的筛选栏（画像标签下拉 + 画像标记输入 + 导出按钮）和 **「AI 画像」列** ✅
- 列表里 user 8 显示 `自然风光×9 历史古建×2` + 摘要；没有画像的用户显示**「暂无画像」** ✅
- **标签下拉加载出完整词表，实测 9 个选项**，顺序正确 ✅
- 选「自然风光」→ 查询 → **共 1 条**，画像列展示标签+权重 ✅
- 点「画像」→ 抽屉渲染出全部 7 个分区：基础信息 / 受控偏好标签 / 结构化偏好 / 自由标签 /
  硬性约束 / AI 画像摘要 / 运营备注-标记，底部还有那句
  「人工修改即时生效，用户下次生成行程时会被 AI 重写覆盖（当前不做锁定）」✅
- 点「编辑画像」→ 标签行（下拉 + 权重数字输入 + 删除）、添加按钮、4 个结构化控件全部正常渲染 ✅
- 改权重 9→10 → 保存 → 出现「画像已保存」，**且抽屉回读到服务端的 ×10**（不是本地拼的值）✅
- 点「保存备注」→ 出现「备注已保存」，无应用报错 ✅
- 点「导出」→ 触发 CSV 下载，出现「已导出 1 条」✅
- **全程控制台 0 错误、后端日志 0 条 4xx/5xx、0 Exception** ✅（favicon 之类噪音未出现）

### G. `profile_note` 是否真的进了 prompt（12 项断言，全过）

这是说明书点名的第 1 个坑，专门验了：

- Python `BaseInfo` **真的声明了** `profile_note`，且有默认值（老请求不带也兼容）✅
- **Java 侧实际发的那个 JSON 请求体**（含 `profile_note`）能被解析且字段**存活** ✅
- 走 `main.py` 同一路径 `json.dumps(base_info.model_dump())` 后，**画像文本在序列化结果里** ✅
- **对话 system prompt 里含画像文本**，且含我加的说明句 ✅
- **规划 system prompt 里同样含**说明句与画像文本 ✅
- 反向确认：`context_note` 确实会被丢弃（已知问题，非本次引入）✅

### H. CSV 导出（Excel 兼容性实测）

在浏览器里挂钩 `URL.createObjectURL` 拿到导出的 Blob，读**原始字节**验证：

```
first6 = ef bb bf 22 49 44     ← EF BB BF = UTF-8 BOM，后面是 "ID
mime   = text/csv;charset=utf-8
表头   = "ID","用户名","昵称","手机号","状态","注册时间","画像标签","画像摘要",
         "对话轮数","画像更新时间","更新来源","运营备注","运营标记"
数据行 = "8","e2e_profile_u1","画像测试用户",…,"自然风光(10)、历史古建(2)",…
```

BOM 在、字段用双引号包裹、内部引号会被转义（`"` → `""`）⇒ Excel 打开中文不乱码、摘要里的逗号不会串列。

> 一个测量陷阱（记下来免得后面误判）：用 `blob.text()` 读会**看不见 BOM**——
> 按规范 `Blob.text()` 走 UTF-8 decode，会吃掉开头的 BOM。必须读 `arrayBuffer()` 看原始字节。

---

## 八、本次**没有**验证到的部分（需要 agent 服务）

真实对话链路依赖 Python agent（需要 `MAAS_API_KEY` 且会产生模型费用），本机 agent 未运行，
所以下面这些**没有端到端跑过**：

1. **真实对话后规则累加是否写库**：`/travel/chat` 先调 agent，agent 失败就不会走到累加。
   - 已用替代证据覆盖：规则抽取本身 38 项断言全过；累加依赖的
     `updateById` 部分更新语义已通过「用户端开关」用例在真实库上验证（只写 `use_preference`、
     其余字段不动）；实体与库列名逐列比对一致。
2. **摘要重写的真实模型调用**：需要按第二节配上 `profile.summary.*` 才能跑。
   - 已覆盖：未配置时的降级路径（跳过 + warn）在代码里是唯一出口，且 `isConfigured()` 为空即返回 null；
     模型输出的宽松解析、词表校验、受控标签过滤、`summary_time` 处理都有单测级断言。
3. **`profile_note` 对 AI 回复质量的实际影响**（模型是否真的不重复追问）——需要真实模型调用。

---

## 九、改动文件清单

**新增（后端）**

```
backend/gkv-pojo/.../entity/UserProfile.java
backend/gkv-pojo/.../dto/ProfileTagDTO.java
backend/gkv-pojo/.../dto/ProfileSummaryDTO.java
backend/gkv-pojo/.../dto/UserProfileEditDTO.java
backend/gkv-pojo/.../dto/UserProfileRemarkDTO.java
backend/gkv-pojo/.../dto/UserProfileSwitchDTO.java
backend/gkv-pojo/.../vo/UserProfileVO.java
backend/gkv-common/.../constant/ProfileConstant.java      ← 受控词表 + 关键词表（确定性来源）
backend/gkv-common/.../utils/ProfileExtractor.java        ← 规则抽取器
backend/gkv-common/.../utils/ProfileTagUtil.java          ← 标签 JSON 固定顺序序列化 + 指纹
backend/gkv-common/.../utils/ProfileNoteBuilder.java      ← 回灌文本（唯一出口）
backend/gkv-common/.../utils/ProfileSummaryClient.java    ← 独立摘要模型客户端（含降级）
backend/gkv-server/.../mapper/UserProfileMapper.java
backend/gkv-server/.../service/UserProfileService.java + impl/UserProfileServiceImpl.java
backend/gkv-server/.../service/AdminUserProfileService.java + impl/AdminUserProfileServiceImpl.java
backend/gkv-server/.../service/ProfileRewriteService.java  ← 独立线程池的摘要重写
backend/gkv-server/.../controller/admin/AdminUserProfileController.java
backend/gkv-server/.../controller/user/UserProfileController.java
部署表结构/V2.1__create_user_profile.sql
部署表结构/V2.2__alter_chat_history_add_user_id.sql
```

**修改（后端）**：`WebMvcConfiguration`（白名单精确化）、`JwtTokenUserInterceptor`（补 ThreadLocal 清理）、
`ChatServiceImpl`（注入画像 / 落 user_id / 规则累加）、`PlanTaskService`（请求线程内注入 + 缓存 key 加指纹）、
`TravelPlanCacheService`（key 加指纹重载）、`TripPlanServiceImpl`（保存成功后异步触发重写）、
`AdminUserServiceImpl` + `AdminUserService`（画像筛选 + 批量补字段 + 导出）、`AdminUserController`（导出接口）、
`UserProfile` 相关 DTO/VO、`AgentHttpUtil`（透传 `profile_note`）、`application.yml`（摘要模型配置）。

**新增（Python）**：`main.py` 的 `BaseInfo` 加 `profile_note` 字段（**必须显式声明**）。
**修改（Python）**：4 个 prompt 各加一句画像说明（`chat_prompt.txt`、`plan_prompt.txt`、
`plan_prompt_frame.txt`、`plan_prompt_detail.txt`）。

**新增（管理端）**：`admin-web/src/api/userProfile.ts`。
**修改（管理端）**：`views/user/index.vue`（筛选 + AI 画像列 + 画像抽屉）、`utils/request.ts`
（`downloadCsv`，带 BOM）、`types/index.ts`、`api/user.ts`。

**新增（用户端）**：`frontend/src/api/profile.ts`。
**修改（用户端）**：`pages/ai/chat.vue`（输入框上方的「使用 AI 记住的偏好」开关 + 登录拦截）、
`api/trip.ts`（三个接口去掉 `skipAuth`）。

**dev 配置（为本地验证顺手修的）**

- `admin-web/vite.config.ts`：去掉 `'/user'` 代理规则。管理端**没有任何 `/user/**` 接口调用**
  （全仓 grep 只有 SPA 路由 `/user`），而这条代理会把 SPA 的 `/user`（用户管理页）转发到后端，
  导致在该路由硬刷新时看到后端的 Whitelabel 404 页。生产 nginx 只代理 `/admin/`，所以只有开发环境会中招。
- `frontend/.env.development.local`（**新增，已被 .gitignore 忽略**）：`VITE_USE_MOCK=false`，
  让 `npm run dev:h5` 连真实后端而不是走 mock。不写这个文件的话 `USE_MOCK` 默认为 `true`，
  H5 开发页全是假数据，验证不到真实链路。

**文档**：`deploy/部署说明.md`（建表 1.1/1.2、摘要模型 2.1、行为变更提示、接口清单、
`context_note` 更正、两处环境漂移修正）、本文件。

---

## 十、本地验证环境（已拉起，供你点着验）

在 `127.0.0.1` 上起了三个进程，直接开浏览器就能点：

| 服务 | 地址 | 说明 |
|---|---|---|
| 后端 | http://127.0.0.1:8080 | Spring Boot jar（本次新打包，98.9MB） |
| 管理端 | http://127.0.0.1:5173 | admin-web dev server。**管理端账号 `admin` / `admin123`** |
| 用户端 H5 | http://127.0.0.1:5174 | uni-app H5 dev（已关 mock，连真实后端）。**测试账号 `rogers` / `123456`** |

> H5 默认也抢 5173（uni 会绑 0.0.0.0，和 admin 的 127.0.0.1 并存，容易混），所以显式启在了 **5174**。

### 建议的验证路径

**管理端（画像板块，主要交付物）**
1. 打开 http://127.0.0.1:5173/user （已登录会直接进；未登录用 `admin/admin123`）
2. 列表里 **user 8（e2e_profile_u1）** 已经有画像：`自然风光×9 历史古建×2` + 摘要；
   其余用户显示「暂无画像」
3. 筛选栏选「画像标签 = 自然风光」→ 查询 → 应只剩 1 条；换个没数据的标签 → 应为 0 条（不会变全量）
4. 点「画像」→ 抽屉 7 个分区；点「编辑画像」→ 标签行/权重/四个下拉；改完保存 → 提示后**回读服务端值**
5. 点「导出」→ 下载 CSV，用 Excel 打开中文不乱码
6. 「保存备注」是独立按钮，不会连带提交画像内容

**用户端（开关）**
1. 打开 http://127.0.0.1:5174 → 用 `rogers/123456` 登录
2. 任意入口进「AI行程助手」对话页（或直接改 hash 到 `#/pages/ai/chat`）
3. 输入框上方有「**使用 AI 记住的偏好**」开关，默认开；点一下会落库并提示
   （开关只关"回灌"，后台仍继续沉淀）
4. 此时去管理端看 rogers（id=3）会出现一行画像；验证完可以用这段话清掉：
   ```sql
   DELETE FROM geek012.user_profile WHERE user_id = 3;
   ```

### 想验「AI 部分」还需要补两样

| 要验的东西 | 前置条件 |
|---|---|
| **真实对话**（`/travel/chat`、生成行程、对话后画像标签权重自动累加、`profile_note` 回灌） | **需要把 Python agent 跑起来**：`cd travel_self_agent && MAAS_API_KEY=<你的百炼Key> python main.py`（监听 8002）。现在 agent 没起，且环境变量里没有 `MAAS_API_KEY`，所以这几个接口会报连接失败——**不是本次代码的问题** |
| **AI 文字摘要**（保存行程后异步重写摘要、来源标 `model`） | 需要按第二节配上 `profile.summary.url / api-key / model`。不配就是**跳过 + 打 warn**，其余功能正常 |

后端与 H5 的 dev server 我留着在跑；要停就关掉对应终端进程（或告诉我）。


---

## 十一、线上部署记录（2026-09-14，服务器 175.178.130.86）

### 已部署并验证

| 项目 | 内容 | 验证方式 |
|---|---|---|
| 后端 jar | `/www/geek012/gkv-server.jar` 98980138 字节 @20:51，进程 20:52 重启 | `/travel/history` 无 token **返回 401**（旧 jar 是匿名放行的 200）——这是新代码在跑的决定性判据 |
| 数据库 DDL | `V2.1` 建 `user_profile`（18 列）、`V2.2` 加 `chat_history.user_id` | 查 `information_schema`：表在、18 列、**列名与实体 `@TableField` 映射完全一致**；`chat_history` 391 行存量数据完好 |
| 管理端静态页 | `/www/geek012/admin_h5`（32 文件） | 8082 返回 200；产物内含 `AI 画像` 与 `user-profile` |
| 用户端 H5 静态页 | `/www/geek012/frontend_h5`（82 文件）**注意：H5 挂在 80 端口，不是 8080** | 80 端口返回 200；产物内含新开关文案「使用 AI 记住的偏好」，API 指向 `175.178.130.86:8080` |
| 匿名白名单行为 | `/travel/plan/status/x` 200、`/travel/aiPlan` 200 仍匿名；`/travel/history`、`/user/profile/switch`、`/admin/user-profile/1` 均 401 | 逐个 curl 实测 |

**生产数据未受影响**：`sys_user` 76、`trip_plan` 56、`chat_history` 391 行，DDL 全是增量（建表 / 加可空列），无删除无改动。

**回滚材料**（部署前已做）：
- `/www/geek012/gkv-server.jar.bak_<时间戳>`
- `/www/geek012/admin_h5.bak_<时间戳>`、`/www/geek012/frontend_h5.bak_<时间戳>`
- 另有替换前的 `.pre` 目录（替换后已清理）

**新后端不会打挂 agent**：对线上 agent 带 `profile_note` 实测 `POST /api/chat` → **HTTP 200** 且正常回复
（线上 `BaseInfo` 无 `model_config`，pydantic 默认 `extra='ignore'`，多传字段被忽略而非报错）。

### ⚠️ 未部署：agent 的 prompt 说明句 —— 而且**不应该照本地文件部署**

你同意「一起部署 agent prompt，影响 DXY 可接受」。但实际核对后发现一个必须先报告的事实：

> **本地仓库的 `travel_self_agent/` 与线上跑的不是同一份代码，连 prompt 文件名都不一样。**

| 对比项 | 本地仓库副本 | 线上 `/www/DXY/educate/travel_self_agent`（真正在跑） |
|---|---|---|
| prompt 位置 | `agent/prompt/`：`chat_prompt.txt`(1432B)、`plan_prompt.txt`、`plan_prompt_frame.txt`、`plan_prompt_detail.txt` | `agent/prompts/`：`chatter_prompt.txt`、`planner_prompt.txt`、`researcher_prompt.txt`、`inspector_prompt.txt` |
| `BaseInfo` 字段 | 无 `profile_note`、无 `context_note` | **有 `context_note`**、有 `transport_preference`；无 `profile_note` |
| 架构 | 简单 chat / plan | researcher→planner→inspector 管线 + 门票核实 + 预算估算（`travel_agent.py` 约 100KB，有 09-14 09:09 的备份） |

所以「拷贝我那 4 个文件过去」**在线上不会被读到**，属于无效动作；而如果去覆盖 `agent/prompts/*.txt`，
那是把线上这套（可能被定制过的）提示词换成我仓库的旧版本，**影响面远大于"加一句说明"**。
基于「不覆盖我没有创建、且与描述不符的目标」的原则，**我一个字都没动 agent**。

### 因此：AI 侧回灌在线上目前是「静默降级」

后端已经会往 `base_info.profile_note` 里灌画像文本（可在 8081 请求日志里看到），
但线上 agent 的 `BaseInfo` 没声明这个字段 → **pydantic 直接丢弃** → prompt 里没有画像。
表现为：功能不报错、画像表照常沉淀、管理端照常看到画像，**但 AI 不会因此少问已知偏好**。

要在线上真正生效，需要在**线上那份代码**里做两处小改动（建议由熟悉该 agent 的人来做）：

1. `/www/DXY/educate/travel_self_agent/main.py` 的 `BaseInfo` 增加
   `profile_note: Optional[str] = Field(default="", description="用户历史画像，后端注入")`
   （**必须显式声明**，否则就是现在这个静默丢弃的状态）；
2. 在 `agent/prompts/chatter_prompt.txt` 与 `agent/prompts/planner_prompt.txt` 里各加一句：
   「`base_info` 里的 `profile_note` 非空时是用户历史画像，请直接沿用其中已知偏好与硬性约束，
   不要重复追问已知信息；为空则按新用户处理。」

这两处改完需要重启 agent（`cd /www/DXY/educate && source venv/bin/activate && nohup python3 -m travel_self_agent.main >> app.log 2>&1 < /dev/null &`），
**会同时重启 DXY 的 agent**（同一个进程）。

### AI 文字摘要：线上仍是关闭状态

线上外部配置 `/www/geek012/config/application-dev.yml` 里没有 `profile.*`，
jar 内 `application.yml` 的默认值是空 → 摘要重写**跳过 + 打 warn**。
要开启需在外部配置里补上 `profile.summary.url / api-key / model` 后重启后端。
日志关键字：`ProfileSummary` / `ProfileRewrite`。

### 部署方式说明（脚本没直接用）

`deploy/deploy.sh` 与 `deploy/deploy_admin.sh` **当前不可直接运行**：它们的
`PROJECT_ROOT` 取脚本自身所在目录（`deploy/`），于是 `LOCAL_JAR` / `LOCAL_H5` / `LOCAL_ADMIN_DIST`
都解析成 `deploy/backend/...` 这类不存在的路径（脚本只有在仓库根目录时才成立）。
另外它们去 `backend/sql/` 找 SQL，而该目录不存在（迁移都在 `部署表结构/`）。
本次是按脚本的**语义**手工分步执行的，没改这两个脚本。要修的话是两处：
把 `PROJECT_ROOT` 解析成仓库根、把 SQL 目录指向 `部署表结构/`。
